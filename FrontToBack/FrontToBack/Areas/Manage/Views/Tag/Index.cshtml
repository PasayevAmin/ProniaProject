@model DashboardVM
@{
    int count = 0;
}


<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">

            <div class="col-lg-12 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Tag Table</h4>
                        <a asp-controller="tag" asp-action="create" class="btn btn-success">Create</a>
                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>
                                            No
                                        </th>
                                        <th>
                                            Name
                                        </th>
                                        <th>
                                            Products Count
                                        </th>
                                        <th>
                                            Settings

                                        </th>

                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.Tags)
                                    {
                                        count++;

                                        <tr>
                                            <td>
                                                @count
                                            </td>

                                            <td>
                                                @item.Name
                                            </td>
                                            <td>
                                                @item.ProductTags.Count
                                            </td>
                                            <td>
                                                <a asp-controller="tag" asp-action="update" asp-route-Id="@item.Id" class="btn btn-warning btn-info"><i class="mdi mdi-border-color"></i></a>
                                                <a asp-controller="tag" asp-action="deletes" asp-route-Id="@item.Id" class="btn btn-danger btn-info"><i class="mdi mdi-delete-sweep"></i></a>
                                                <a asp-controller="tag" asp-action="details" asp-route-Id="@item.Id" class="btn btn-primary"><i class="mdi mdi-book-open-variant"></i></a>
                                            </td>
                                        </tr>

                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>


        </div>
    </div>
